# Secrets - never commit
.env
.env.local
*.env
!.env.example

# Dependencies
node_modules/

# Rust build artifacts (huge, regenerable)
target/

# Build output
dist/
dist-ssr/
*.local

# Logs
*.log
npm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Design references (images dropped for Claude to review, never committed)
design-references/

# Audit round prompts/findings — detailed exploit walkthroughs against
# not-yet-fixed vulnerabilities. Repo is public; never commit these until
# the underlying finding is actually resolved and shipped.
docs/audit-prompts/

# Local monitoring tooling — reveals bot-vs-real-user detection logic
# (e.g. the KeplrWalletMobile signal) that's better kept off a public repo.
scripts/cloudflare-report.py
