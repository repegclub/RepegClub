// Real project ID needed for the mobile/QR connect path (WalletConnect) to
// actually work - a plain browser-extension connection never touches it.
// Register free at https://cloud.reown.com (WalletConnect Cloud's new
// name), create a project, and paste its ID here. Public identifier, not a
// secret - same category as the EmailJS/Skip Go IDs already committed
// elsewhere in this project (feedbackConfig.ts/onrampConfig.ts).
export const WC_PROJECT_ID = "f72d5273848eaf9e02cf03c1f74020f3";
