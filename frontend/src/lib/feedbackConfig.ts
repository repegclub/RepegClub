// EmailJS (emailjs.com) sends the in-app feedback form straight to
// repegclub@gmail.com without a backend - these 3 IDs are public by design
// (EmailJS scopes sending through the dashboard-configured service/template,
// not through these values), so it's fine to ship them in frontend code.
// Get them from the EmailJS dashboard: Email Services (service ID),
// Email Templates (template ID), Account > General (public key).
export const EMAILJS_SERVICE_ID = "service_f60956c";
export const EMAILJS_TEMPLATE_ID = "template_71cx9uo";
export const EMAILJS_PUBLIC_KEY = "I5T-mv13ZmF-uquVF";
